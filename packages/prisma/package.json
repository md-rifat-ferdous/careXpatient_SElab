{
  "name": "@carexpatient/prisma",
  "version": "1.0.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "generate": "prisma generate",
    "push": "prisma db push",
    "push:force": "prisma db push --accept-data-loss",
    "studio": "prisma studio",
    "validate": "prisma validate"
  },
  "dependencies": {
    "@prisma/client": "^6.2.0"
  },
  "devDependencies": {
    "prisma": "^6.2.0"
  }
}
